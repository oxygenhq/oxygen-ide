{
  "name": "oxygenide",
  "productName": "Oxygen IDE",
  "version": "1.0.0",
  "description": "IDE for Oxygen automation framework.",
  "main": "./main/main.prod.js",
  "author": {
    "name": "cloudbeat.io",
    "email": "ndimer@cloudbeat.io"
  },
  "scripts": {
    "electron-rebuild": "cd ../ && node -r @babel/register ./internals/scripts/ElectronRebuild.js",
    "postinstall": "npm run electron-rebuild"
  },
  "license": "GPL-3.0",
  "dependencies": {
    "chokidar": "3.5.2",
    "oxygen-cli": "1.21.0-beta.8",
    "xml2json": "0.12.0"
  },
  "@comment dependencies": [
    "@types/express and @types/qs are peer dependencies in oxygen-cli/twilio. they need to be included to avoid 'npm ls' failing due to missing deps"
  ],
  "devDependencies": {
    "@types/express": "4.17.11",
    "@types/qs": "6.9.4"
  }
}
